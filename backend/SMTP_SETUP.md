# Настройка SMTP для отправки email

## Быстрая настройка для Gmail

1. Включите двухфакторную аутентификацию в вашем Google аккаунте:
   - Перейдите в https://myaccount.google.com/security
   - Включите "Двухэтапную аутентификацию"

2. Создайте пароль приложения:
   - Перейдите в https://myaccount.google.com/apppasswords
   - Выберите "Почта" и "Другое устройство"
   - Введите название: "Liberty Backend"
   - Скопируйте сгенерированный 16-значный пароль

3. Обновите файл `.env`:
```env
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=ваш-email@gmail.com
SMTP_PASSWORD=ваш-16-значный-пароль-приложения
SMTP_FROM_EMAIL=ваш-email@gmail.com
SMTP_FROM_NAME=Liberty
```

4. Перезапустите сервер:
```bash
# Остановите текущий процесс (Ctrl+C)
# Затем запустите снова:
uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

## Альтернативные SMTP сервисы

### Yandex Mail
```env
SMTP_HOST=smtp.yandex.ru
SMTP_PORT=465
SMTP_USER=ваш-email@yandex.ru
SMTP_PASSWORD=ваш-пароль-приложения
SMTP_FROM_EMAIL=ваш-email@yandex.ru
SMTP_FROM_NAME=Liberty
```

### Mail.ru
```env
SMTP_HOST=smtp.mail.ru
SMTP_PORT=465
SMTP_USER=ваш-email@mail.ru
SMTP_PASSWORD=ваш-пароль-приложения
SMTP_FROM_EMAIL=ваш-email@mail.ru
SMTP_FROM_NAME=Liberty
```

## Режим разработки (без SMTP)

Если SMTP не настроен, код подтверждения будет выводиться в консоль сервера:
```
============================================================
📧 [LIBERTY] КОД ПОДТВЕРЖДЕНИЯ
============================================================
Email: user@example.com
Код: 12345
============================================================
```

Смотрите консоль сервера, где запущен `uvicorn`, чтобы увидеть код.

