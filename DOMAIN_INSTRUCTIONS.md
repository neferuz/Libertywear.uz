# Инструкция по подключению домена libertywear.uz

## ✅ Что уже сделано:

1. ✅ Обновлены CORS настройки в backend для поддержки домена
2. ✅ Создана конфигурация Nginx
3. ✅ Создан скрипт автоматической настройки

## 📋 Пошаговая инструкция:

### Шаг 1: Настройка DNS записей

В панели управления доменом (webspace.uz) добавьте следующие **A записи**:

```
Тип: A
Имя: @ (или оставьте пустым для основного домена)
Значение: 147.45.155.163
TTL: 3600

Тип: A
Имя: www
Значение: 147.45.155.163
TTL: 3600

Тип: A
Имя: admin
Значение: 147.45.155.163
TTL: 3600

Тип: A
Имя: api
Значение: 147.45.155.163
TTL: 3600
```

**NS записи уже настроены:**
- dns1.webspace.uz
- dns2.webspace.uz
- dns3.webspace.uz

### Шаг 2: Установка и настройка Nginx на сервере

Выполните на сервере:

```bash
cd /root/liberty
sudo ./setup_domain.sh
```

Или вручную:

```bash
# Установка nginx
sudo apt update
sudo apt install nginx -y

# Установка certbot для SSL
sudo apt install certbot python3-certbot-nginx -y

# Копирование конфигурации
sudo cp nginx-libertywear.conf /etc/nginx/sites-available/libertywear.uz
sudo ln -sf /etc/nginx/sites-available/libertywear.uz /etc/nginx/sites-enabled/
sudo rm -f /etc/nginx/sites-enabled/default

# Проверка и перезапуск
sudo nginx -t
sudo systemctl restart nginx
sudo systemctl enable nginx
```

### Шаг 3: Ожидание распространения DNS

Подождите **5-30 минут** после настройки DNS записей. Проверить можно командой:

```bash
dig libertywear.uz
# или
nslookup libertywear.uz
```

### Шаг 4: Получение SSL сертификата

После того как DNS записи распространились, выполните:

```bash
sudo certbot --nginx -d libertywear.uz -d www.libertywear.uz -d admin.libertywear.uz -d api.libertywear.uz
```

Certbot автоматически:
- Получит SSL сертификат от Let's Encrypt
- Настроит HTTPS
- Настроит автоматическое обновление сертификата

### Шаг 5: Перезапуск backend

После настройки домена перезапустите backend:

```bash
cd /root/liberty/backend
# Остановите текущий процесс
pkill -f "python.*main.py"
# Запустите заново
source venv/bin/activate
python main.py &
```

## 🌐 Структура доменов:

- **https://libertywear.uz** или **https://www.libertywear.uz** → Frontend (магазин)
- **https://admin.libertywear.uz** → Admin панель
- **https://api.libertywear.uz** → Backend API

## 🔧 Проверка работы:

1. Проверьте доступность домена:
   ```bash
   curl -I https://libertywear.uz
   ```

2. Проверьте API:
   ```bash
   curl https://api.libertywear.uz/health
   ```

3. Откройте в браузере:
   - https://libertywear.uz
   - https://admin.libertywear.uz
   - https://api.libertywear.uz/docs

## ⚠️ Важно:

- Убедитесь, что порты 80 и 443 открыты в firewall
- После получения SSL сертификата все HTTP запросы будут перенаправляться на HTTPS
- CORS настройки уже обновлены в backend/main.py

## 📝 Обновление конфигурации frontend (опционально):

Если нужно обновить BASE_URL в frontend для работы с доменом, измените файлы:
- `zara/src/constants/config.js`
- `admin/src/constants/config.js`

Но обычно автоматическое определение работает корректно.

